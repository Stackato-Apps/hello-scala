# How to use scala application.

Building the Application
------------------------

To build the application, make sure you have [Simple Build Tool] installed.
Then, *cd* into the root directory and execute:

	sbt clean compile stage

Pushing to Stackato
------------------------

    	stackato push -n
